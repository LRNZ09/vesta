# Vaultwarden admin-panel token. The compose file reads this host-side variable
# and passes it into the container as ADMIN_TOKEN (which guards /admin).
# Generate a hash:  docker run --rm vaultwarden/server \
#   /vaultwarden hash      (or: openssl rand -base64 48 for a plain token)
VAULTWARDEN_ADMIN_TOKEN=
